(executables
 ((names (main))
  (libraries (cinaps str))
  (link_flags (-linkall))
  (link_executables false)
  (preprocess no_preprocessing)))


(install
 ((section bin)
  (files ((main.bc as cinaps)))))


(jbuild_version 1)
